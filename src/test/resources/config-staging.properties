# staging environment
# Only the staging FRONTEND url is known (https://ventura.staging.csniico.com/login) - the
# staging API host has not been confirmed, so it is intentionally left blank here rather than
# guessed. Fill this in and re-run with -Denv=staging once you have it.
base.uri=
